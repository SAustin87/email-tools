console.log('hello world')


/* const fs = require('fs');

const testFolder = '../contentSections/componentModules/';
const fs = require('fs');
fs.readdir(testFolder, (err, files) => {
  files.forEach(file => {
    console.log(file);
  });
}) */
